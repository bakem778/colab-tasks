name: Ping Replit on Push

on:
  push:
    paths:
      - 'tasks.json'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Replit
        run: |
          curl -s https://c3fe5641-b22b-4a8b-8acf-40996cf82e9e-00-13bevjm464pw4.sisko.replit.dev/
